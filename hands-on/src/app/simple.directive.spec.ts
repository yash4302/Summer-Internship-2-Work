import { SimpleDirective } from './simple.directive';

describe('SimpleDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleDirective();
    expect(directive).toBeTruthy();
  });
});
