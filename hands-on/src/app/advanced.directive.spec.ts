import { AdvancedDirective } from './advanced.directive';

describe('AdvancedDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancedDirective();
    expect(directive).toBeTruthy();
  });
});
